---
title: 'learning mdx'
description: 'MDX is the easiest way to write content'
author: 'Hamed'
date: '2022-02-12'
---

import NoteLayout from '../../components/layout/NoteLayout'
import Test from '../../components/home/Test'

export const meta = {
  title: 'learning mdx',
  description: 'MDX is the easiest way to write content',
  author: 'Hamed',
  date: '2022-02-12',
}

# My MDX page

## This is a test component:

<Test title={meta.title} />

This is a list in markdown:

- One
- Two
- Three

check out the `JSX`

<div className='text-white bg-emerald-400 rounded px-4'>
  This is using `TailwindCSS`
</div>

```js
function greet() {
  console.log('Hello')
}
```

How about some HTML?

```html
<!DOCTYPE html>

<div>
  <h1>This is my Title</h1>
</div>
```

let's spice it up with some CSS

{meta.author} - {new Date(meta.date).toLocaleString('en-CA', {day: 'numeric', month: 'long', year: 'numeric'})}

export default ({ children }) => <NoteLayout meta={meta}>{children}</NoteLayout>
